# Verified against services/edge/requirements.txt's target Python (3.12) and
# against an actual resolved environment: these three interoperate cleanly
# and satisfy rfdetr 1.9.1's own pins (aiortc/av/aiohttp are otherwise
# unrelated to rfdetr — WebRTC is a standard protocol, so this worker does
# NOT need to match services/edge's aiortc version to interoperate with it).
aiortc==1.14.0
aiohttp==3.13.5
av==14.2.0
numpy==2.2.6
python-dotenv==1.1.0
Pillow>=10.4.0

# GPU inference stack. Install a CUDA-matched torch/torchvision build FIRST —
# not pinned here because the right wheel depends on your GPU driver, e.g.:
#   pip install "torch>=2.2" "torchvision>=0.17" --index-url https://download.pytorch.org/whl/cu121
# then install the rest, which will see torch/torchvision already satisfied:
#   pip install -r requirements.txt
#
# rfdetr==1.9.1 is the first version confirmed (by inspecting its source) to
# expose the RFDETR.from_checkpoint() / model.inference() API this worker
# uses — capstone_inference.ipynb's `%pip install -U rfdetr` floats to
# whatever is newest, which pulled in this API at the time the notebook was
# authored. rfdetr==1.6.5, still resolvable from PyPI, predates it and will
# NOT work with this code.
rfdetr==1.9.1
supervision>=0.29.0
